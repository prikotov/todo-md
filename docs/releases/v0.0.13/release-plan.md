# План релиза v0.0.13

## Метаданные

- Тег релиза: `v0.0.13`
- Тип повышения версии: `patch`
- Обоснование выбранной версии: совместимое исправление CLI-валидации
- Линия релиза: `release/0.0`
- Исходная ветка: `master` (`1b60af9`)
- Ответственный: Владелец проекта
- Плановая дата deploy: 2026-09-12

## Состав

- Включённые PR: [#32](https://github.com/prikotov/todo-md/pull/32)
- Включённые задачи: `TASK-todo-md-validate-by-id`
- Вне состава релиза: реализация `TASK-todo-md-sdd-comparison-add-kiro` и другие изменения после `1b60af9`

## Риски

- Основные риски: скрипты, ошибочно передающие несуществующий путь, теперь получают ожидаемый ненулевой код выхода
- Наличие миграций данных: нет
- Порядок применения миграций: не требуется
- Риск окна несовместимости: отсутствует
- Замечания по обратной совместимости: существующие вызовы с файлами и директориями сохранены; добавлена проверка по ID

## Порядок deploy

1. Опубликовать тег `v0.0.13`.
2. Создать GitHub Release `v0.0.13`.
3. Дождаться доступности версии через Composer VCS metadata.

## Проверки перед deploy

- [ ] Тег релиза запушен в `origin`
- [x] Изменения в env не требуются
- [x] Миграции отсутствуют
- [x] `composer validate --strict` проходит
- [x] `make check` проходит

## Проверки после deploy

- Установить `prikotov/todo-md:v0.0.13` в тестовый проект через Composer.
- Выполнить `php vendor/bin/todo-md validate TASK-example` для задачи в `todo/backlog/`.
- Убедиться, что неизвестный ID возвращает код `1`.
- Проверить версию и git SHA опубликованного тега.

## Действия при проблеме после релиза

- Откат: не используется
- Стратегия исправления: `hotfix` / `patch release`
- Ответственный инженер: Владелец проекта
- Канал коммуникации / задача: GitHub Issue и задача в `todo/`

## Заметки

- Пакет не содержит Web, Workers, миграций или env-конфигурации.
